---
import Layout from '../layouts/Layout.astro';
import { UnifiedSearch } from '../components/unified-search';
import { url } from '../lib/url';
import TypstRender from '../components/typst-render/index.astro';

// Sample notations for the hero section
const sampleNotations = [
  { typst: '$lambda x. e$', name: 'Lambda Abstraction' },
  { typst: '$x in A$', name: 'Element Membership' },
  { typst: '$sum_(i=1)^n i$', name: 'Summation' },
];
---

<Layout title="Home">
  <div class="py-12">
    <!-- Hero Section -->
    <div class="text-center mb-12">
      <h1 class="text-5xl font-bold text-gray-900 sm:text-6xl md:text-7xl">
        Ubiquitongue
      </h1>
      <p class="mt-4 max-w-2xl mx-auto text-lg text-gray-600 sm:text-xl">
        A review of notational conflicts that could be the leading cause of
        murder in researchers.
      </p>
      
      <!-- Sample Notations Display -->
      <div class="mt-12 flex justify-center items-center gap-6 flex-wrap">
        {sampleNotations.map((notation, index) => (
          <div class="group relative">
            <div class="card-shadow bg-white rounded-xl p-6 border border-gray-200">
              <div class="text-3xl mb-3 math-content">
                <TypstRender src={notation.typst} />
              </div>
              <div class="text-sm font-medium text-gray-700">{notation.name}</div>
            </div>
            {index < sampleNotations.length - 1 && (
              <span class="hidden lg:inline-block absolute -right-3 top-1/2 -translate-y-1/2 text-gray-300 text-2xl">
                ≈
              </span>
            )}
          </div>
        ))}
      </div>
    </div>

    <!-- Search Section -->
    <div class="max-w-4xl mx-auto mb-16 mt-16">
      <div class="card-shadow bg-white rounded-2xl p-8 border border-gray-100">
        <h2 class="text-3xl font-bold text-gray-900 mb-6 text-center">Search Notations</h2>
        <UnifiedSearch client:load />
      </div>
    </div>

    <!-- Key Concepts -->
    <div class="max-w-5xl mx-auto mb-16">
      <h2 class="text-3xl font-bold text-gray-900 text-center mb-10">
        Bridging Syntax and Semantics
      </h2>
      <div class="grid grid-cols-1 gap-8 md:grid-cols-2">
        <!-- Semantic Card -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-r from-purple-400 to-purple-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity"></div>
          <div class="relative bg-white rounded-2xl shadow-lg p-8 border border-purple-100">
            <div class="flex items-center gap-3 mb-4">
              <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900">Semantic Meaning</h3>
            </div>
            <p class="text-gray-700 mb-4">
              The conceptual understanding of what a notation represents in mathematical or computational terms.
            </p>
            <div class="bg-purple-50 rounded-lg p-4">
              <p class="text-sm text-gray-700">
                <strong>Example:</strong> "Lambda Abstraction" represents an anonymous function or function literal in lambda calculus
              </p>
            </div>
          </div>
        </div>

        <!-- Syntax Card -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-r from-blue-400 to-blue-600 rounded-2xl blur opacity-20 group-hover:opacity-30 transition-opacity"></div>
          <div class="relative bg-white rounded-2xl shadow-lg p-8 border border-blue-100">
            <div class="flex items-center gap-3 mb-4">
              <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
                </svg>
              </div>
              <h3 class="text-xl font-semibold text-gray-900">Syntactic Form</h3>
            </div>
            <p class="text-gray-700 mb-4">
              The visual or textual representation of how a concept is written in various notation systems.
            </p>
            <div class="bg-blue-50 rounded-lg p-4">
              <p class="text-sm text-gray-700">
                <strong>Example:</strong> Written as <code class="bg-white px-2 py-0.5 rounded border border-blue-200 font-mono">λx.e</code> in Typst or <code class="bg-white px-2 py-0.5 rounded border border-blue-200 font-mono">\lambda x. e</code> in LaTeX
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Navigation Cards -->
    <div class="max-w-4xl mx-auto grid grid-cols-1 gap-6 sm:grid-cols-2 mb-16">
      <a href={url('/notations')} class="card-shadow group relative overflow-hidden bg-white rounded-2xl">
        <div class="absolute inset-0 bg-gradient-to-br from-blue-500/5 via-purple-500/5 to-pink-500/5"></div>
        <div class="relative p-8">
          <div class="flex items-center gap-4 mb-4">
            <div class="w-16 h-16 bg-gradient-to-br from-blue-500/10 to-purple-500/10 rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform">
              <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
              </svg>
            </div>
            <h3 class="text-2xl font-bold text-gray-900">Browse Notations</h3>
          </div>
          <p class="text-gray-600 mb-4">
            Explore our comprehensive collection of mathematical and CS notations with their meanings and representations
          </p>
          <span class="text-primary font-medium group-hover:gap-2 transition-all inline-flex items-center gap-1">
            View all notations
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </span>
        </div>
      </a>

      <a href={url('/papers')} class="card-shadow group relative overflow-hidden bg-white rounded-2xl">
        <div class="absolute inset-0 bg-gradient-to-br from-purple-500/5 via-pink-500/5 to-orange-500/5"></div>
        <div class="relative p-8">
          <div class="flex items-center gap-4 mb-4">
            <div class="w-16 h-16 bg-gradient-to-br from-purple-500/10 to-pink-500/10 rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform">
              <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
            <h3 class="text-2xl font-bold text-gray-900">Browse Papers</h3>
          </div>
          <p class="text-gray-600 mb-4">
            Discover how academic papers use and define various notations in different contexts
          </p>
          <span class="text-secondary font-medium group-hover:gap-2 transition-all inline-flex items-center gap-1">
            View all papers
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </span>
        </div>
      </a>
    </div>

    <!-- About Section -->
    <div class="max-w-4xl mx-auto">
      <div class="bg-gradient-to-br from-gray-50 via-white to-gray-50 rounded-2xl shadow-xl p-12 border border-gray-200">
        <h2 class="text-3xl font-bold text-gray-900 mb-6 text-center">About Ubiquitongue</h2>
        <p class="text-gray-700 text-lg leading-relaxed mb-4 text-center">
          Ubiquitongue is a bidirectional index that connects mathematical notations with their meanings, helping researchers and students navigate the often confusing landscape of academic notation.
        </p>
        <p class="text-gray-600 leading-relaxed text-center">
          By providing clear mappings between semantic meanings and syntactic representations across different papers and contexts, we aim to reduce ambiguity and improve understanding in mathematical and computational literature.
        </p>
      </div>
    </div>
  </div>
</Layout>